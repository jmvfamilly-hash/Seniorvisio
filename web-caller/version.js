// Remplacé au déploiement (voir .github/workflows/deploy-web-caller.yml) par le
// commit et l'horodatage réels : cette valeur ne s'affiche que si le fichier
// n'a pas été généré, en développement local par exemple.
window.PWA_VERSION = "dev (non déployé)";
